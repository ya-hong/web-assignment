extends home-layout

block content
    form(method='POST', action = "/" )
        #form-group
            label 用户名:
            input.form-control(name='name', type='username') 
        #form-group
            label 密码:
            input.form-control(name='password', type='password') 
        br
        input.btn.btn-primary(type='submit', value='Sign in')
        | - or -
        a(href="/signup")
            input.btn.btn-primary(type='buttom', value='Sign up')
